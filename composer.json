{
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^8.4"
    },
    "autoload": {
        "psr-4":{
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": "php -r \"system(join(DIRECTORY_SEPARATOR, array('vendor', 'bin', 'phpunit')) . ' --color=always');\"",
        "son": "./vendor/bin/phpunit tests --colors=always --bootstrap=tests/bootstrap.php"
    }
}
